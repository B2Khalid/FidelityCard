# Porta del backend
PORT=3001

# Sorgente dati: "mock" per la Fase 1, "mysql" per la Fase 2
DATA_SOURCE=mock

# Credenziali database (usate solo quando DATA_SOURCE=mysql)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
